# --- SHARED DATABASE ---
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=doorsteps
MYSQL_USER=user
MYSQL_PASSWORD=password

# --- BACKEND (FastAPI) ---
# Note: Host is 'db' because that's the service name in docker-compose
DATABASE_URL=mysql+pymysql://user:password@db:3306/doorsteps
FIREBASE_SA_KEY_PATH=/app/secrets/fcm-key.json

# --- FRONTEND (Next.js) ---
# Variables must start with NEXT_PUBLIC_ to be visible in the browser
NEXT_PUBLIC_API_URL_LOCAL=http://localhost:8000
NEXT_PUBLIC_API_URL_PROD=https://api.doorstep.nepal.com